export const dynamic = "force-static";

const BODY = "(() => {\n  'use strict';\n  const body = document.body;\n  const toggle = document.getElementById('loreModeToggle');\n  const cinematicView = document.querySelector('[data-lore-view=\"cinematic\"]');\n  const status = document.getElementById('loreModeStatus');\n  const hint = document.getElementById('modeHint');\n  const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)');\n  let readingView = null;\n  let readingPromise = null;\n  let markdownPromise = null;\n  let currentMode = 'cinematic';\n  let chapterObserver = null;\n  let hintTimer = null;\n\n  if (!toggle || !cinematicView) return;\n\n  const storyMap = [\n    { cinematic: '.hero', reading: 'read-uma-vila-pequena-e-uma-vida-simples' },\n    { cinematic: '#casa', reading: 'read-uma-vila-pequena-e-uma-vida-simples' },\n    { cinematic: '#lucky', reading: 'read-lucky' },\n    { cinematic: '#ami', reading: 'read-ami-e-o-primeiro-contato-real-com-o-sobrenatural' },\n    { cinematic: '#ren', reading: 'read-ren' },\n    { cinematic: '#mentoria', reading: 'read-ren' },\n    { cinematic: '#primeiro-exorcismo', reading: 'read-o-primeiro-exorcismo-sozinho' },\n    { cinematic: '#vida', reading: 'read-a-vida-adulta' },\n    { cinematic: '#ausencia', reading: 'read-a-primavera-em-que-ren-nao-voltou' },\n    { cinematic: '#manto', reading: 'read-o-manto' },\n    { cinematic: '#partida', reading: 'read-a-partida' },\n    { cinematic: '#caderno', reading: 'read-quem-e-seika' },\n    { cinematic: '#arquivo', reading: 'read-habitos-e-pequenos-detalhes-de-roleplay' }\n  ];\n\n  const escapeHtml = (value) => value.replace(/[&<>\"']/g, (char) => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',\"'\":'&#039;'}[char]));\n  const inlineMarkdown = (value) => {\n    let text = escapeHtml(value);\n    text = text.replace(/\\*\\*(.+?)\\*\\*/g, '<strong>$1</strong>');\n    text = text.replace(/\\*([^*]+?)\\*/g, '<em>$1</em>');\n    return text;\n  };\n  const slugify = (value) => value.normalize('NFD').replace(/[\\u0300-\\u036f]/g,'').toLowerCase().replace(/[“”\"'.!?(),:;/\\\\]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');\n\n  function extractLongform(markdown) {\n    const marker = '# Backstory Completa';\n    const start = markdown.indexOf(marker);\n    return start >= 0 ? markdown.slice(start + marker.length) : markdown;\n  }\n\n  function parseLongform(markdown) {\n    const lines = extractLongform(markdown).split(/\\r?\\n/);\n    const chapters = [];\n    let current = null;\n    let paragraph = [];\n    let quote = [];\n    let list = null;\n\n    const flushParagraph = () => {\n      if (!current || !paragraph.length) return;\n      current.blocks.push({ type: 'p', text: paragraph.join(' ').trim() });\n      paragraph = [];\n    };\n    const flushQuote = () => {\n      if (!current || !quote.length) return;\n      current.blocks.push({ type: 'quote', lines: [...quote] }); quote = [];\n    };\n    const flushList = () => {\n      if (!current || !list) return;\n      current.blocks.push(list); list = null;\n    };\n    const flushAll = () => { flushParagraph(); flushQuote(); flushList(); };\n    const startChapter = (title, level) => {\n      flushAll();\n      current = { title, level, id: `read-${slugify(title)}`, blocks: [] };\n      chapters.push(current);\n    };\n\n    for (let i = 0; i < lines.length; i += 1) {\n      const raw = lines[i];\n      const line = raw.trim();\n      const h1 = line.match(/^#\\s+(.+)$/);\n      const h2 = line.match(/^##\\s+(.+)$/);\n\n      if (h1) { startChapter(h1[1], 1); continue; }\n      if (h2) {\n        if (!current || current.title === 'Backstory Completa' || chapters.length < 3) startChapter(h2[1], 2);\n        else { flushAll(); current.blocks.push({ type: 'subheading', text: h2[1] }); }\n        continue;\n      }\n      if (!current) continue;\n      if (!line || line === '---') { flushAll(); if (line === '---') current.blocks.push({type:'hr'}); continue; }\n      if (line.startsWith('>')) { flushParagraph(); flushList(); quote.push(line.replace(/^>\\s?/,'')); continue; }\n      flushQuote();\n      const bullet = line.match(/^-\\s+(.+)$/);\n      const numbered = line.match(/^(\\d+)\\.\\s+(.+)$/);\n      if (bullet || numbered) {\n        flushParagraph();\n        const ordered = Boolean(numbered);\n        if (!list || list.ordered !== ordered) { flushList(); list = { type:'list', ordered, items:[] }; }\n        list.items.push(ordered ? numbered[2] : bullet[1]);\n        continue;\n      }\n      if (/^\\s{2,}/.test(raw) && list?.items?.length) {\n        list.items[list.items.length - 1] += ` ${line}`;\n        continue;\n      }\n      flushList(); paragraph.push(line);\n    }\n    flushAll();\n    return chapters.filter((chapter) => chapter.title !== 'Backstory Completa');\n  }\n\n  function renderBlocks(blocks) {\n    return blocks.map((block) => {\n      if (block.type === 'quote') return `<blockquote>${block.lines.map((line) => `<p>${inlineMarkdown(line)}</p>`).join('')}</blockquote>`;\n      if (block.type === 'subheading') return `<h3 class=\"seika-reading-subheading\">${inlineMarkdown(block.text)}</h3>`;\n      if (block.type === 'hr') return '<hr>';\n      if (block.type === 'list') {\n        const tag = block.ordered ? 'ol' : 'ul';\n        return `<${tag}>${block.items.map((item) => `<li>${inlineMarkdown(item)}</li>`).join('')}</${tag}>`;\n      }\n      return `<p>${inlineMarkdown(block.text)}</p>`;\n    }).join('');\n  }\n\n  function chapterFigure(id) {\n    const figures = {\n      'read-lucky': ['https://media.dnd.faysk.dev/lore/seika/a02d6f1ccc772a78f130113b2a7edda88bd0dfbb422be651a75390f8cbcfc7ad/wide-lucky-full.webp', 'Lucky nas ruínas. O apelido ficou; a explicação não.'],\n      'read-ami-e-o-primeiro-contato-real-com-o-sobrenatural': ['https://media.dnd.faysk.dev/lore/seika/8ef2730e025f01bb6ae59dffdcfad1994a4835cd4e43d574cd35e1c02fda0b16/wide-ami-ritual-full.webp', 'O ritual de Ami: o momento em que alguma coisa respondeu.'],\n      'read-o-primeiro-exorcismo-sozinho': ['https://media.dnd.faysk.dev/lore/seika/386ead888a202639588f00b1f07bceebbe83f60f2423963d87f74d9956e530d9/wide-first-exorcism-full.webp', 'O primeiro exorcismo realizado sem Ren por perto.'],\n      'read-a-partida': ['https://media.dnd.faysk.dev/lore/seika/74cf3cdab85ed37eac4a28276864449b9101716e63202ae32a9c314e299793b5/wide-departure-full.webp', 'A partida no começo do verão.'],\n      'read-quem-e-seika': ['https://media.dnd.faysk.dev/lore/seika/40f6c8d86f16c5556d4d467e86fd805a109385f3416b445af8bda8611242ceef/seika-reference.webp', 'Seika aos 23 anos, preparado para a estrada.']\n    };\n    const figure = figures[id];\n    return figure ? `<figure class=\"seika-reading-figure\"><img src=\"${figure[0]}\" alt=\"${escapeHtml(figure[1])}\" loading=\"lazy\"><figcaption>${escapeHtml(figure[1])}</figcaption></figure>` : '';\n  }\n\n  function buildReadingView(markdown) {\n    const chapters = parseLongform(markdown);\n    if (!chapters.length) throw new Error('A backstory completa de Seika não contém capítulos reconhecíveis.');\n    const wordCount = chapters.reduce((sum, chapter) => sum + chapter.blocks.reduce((total, block) => {\n      if (block.type === 'p' || block.type === 'subheading') return total + block.text.split(/\\s+/).filter(Boolean).length;\n      if (block.type === 'quote') return total + block.lines.join(' ').split(/\\s+/).filter(Boolean).length;\n      if (block.type === 'list') return total + block.items.join(' ').split(/\\s+/).filter(Boolean).length;\n      return total;\n    }, 0), 0);\n    const minutes = Math.max(1, Math.round(wordCount / 220));\n    const nav = chapters.map((chapter, index) => `<a href=\"#${chapter.id}\" data-seika-reading-link=\"${chapter.id}\"><span>${String(index + 1).padStart(2,'0')}</span>${escapeHtml(chapter.title)}</a>`).join('');\n    const sections = chapters.map((chapter, index) => `<section class=\"seika-reading-chapter\" id=\"${chapter.id}\" data-seika-reading-chapter><header class=\"seika-reading-chapter-header\"><p>${String(index + 1).padStart(2,'0')} · Antes do Inverno</p><h2>${escapeHtml(chapter.title)}</h2></header><div class=\"seika-reading-prose\">${renderBlocks(chapter.blocks)}</div>${chapterFigure(chapter.id)}</section>`).join('');\n    const main = document.createElement('main');\n    main.id = 'seika-reading-view';\n    main.className = 'seika-reading-view lore-view';\n    main.dataset.loreView = 'reading';\n    main.hidden = true;\n    main.innerHTML = `<div class=\"seika-reading-progress\" aria-hidden=\"true\"><span id=\"seikaReadingProgressBar\"></span></div><header class=\"seika-reading-hero\" id=\"seika-reading-top\"><div class=\"seika-reading-hero-copy\"><p class=\"eyebrow\">HISTÓRIA COMPLETA · ≈ ${minutes} MIN DE LEITURA</p><h1 class=\"seika-reading-title shared-seika-title\">SEIKA</h1><p class=\"seika-reading-subtitle\">Antes do <em>Inverno</em></p><p class=\"seika-reading-deck\">A história completa do herbalista que saiu de casa não para viver uma aventura, mas para encontrar um amigo que deveria ter voltado.</p><a class=\"seika-reading-start\" href=\"#${chapters[0].id}\">Começar a leitura <span aria-hidden=\"true\">↓</span></a></div><div class=\"seika-reading-hero-art\"><div class=\"seika-reading-halo\" aria-hidden=\"true\"></div><img class=\"shared-seika-art\" src=\"https://media.dnd.faysk.dev/lore/seika/ae5e52eaba48fdb16364289a6e5b9e1d9c952142a0b2a67d56b7303f9af01a9d/adult-seika-subject-spirit.webp\" alt=\"Seika adulto acompanhado de fumaça espiritual\"></div></header><div class=\"seika-reading-layout\"><aside class=\"seika-reading-toc\" aria-label=\"Capítulos da história completa\"><p>Capítulos</p><nav>${nav}</nav></aside><article class=\"seika-reading-document\" aria-label=\"História completa de Seika\"><details class=\"seika-reading-mobile-toc\"><summary>Capítulos <span aria-hidden=\"true\">＋</span></summary><nav>${nav}</nav></details>${sections}</article></div><footer class=\"seika-reading-final\"><p>SEIKA · ANTES DO INVERNO</p><strong>“Volto antes do inverno.”</strong><small>Seika acredita nisso quando sai da vila. E, naquele momento, não vê motivo algum para não acreditar.</small><a href=\"#seika-reading-top\">voltar ao começo ↑</a></footer>`;\n    document.querySelector('dialog#lightbox')?.before(main);\n    setupReadingNavigation(main);\n    return main;\n  }\n\n  async function loadMarkdown() {\n    if (!markdownPromise) markdownPromise = fetch('/lore/seika/backstory.md').then((response) => {\n      if (!response.ok) throw new Error(`Falha ao carregar lore completa: ${response.status}`);\n      return response.text();\n    });\n    return markdownPromise;\n  }\n  async function ensureReadingView() {\n    if (readingView) return readingView;\n    if (!readingPromise) readingPromise = loadMarkdown().then((markdown) => { readingView = buildReadingView(markdown); return readingView; });\n    return readingPromise;\n  }\n\n  function nearestCinematicEntry() {\n    const headerOffset = 100; let best = storyMap[0], bestDistance = Infinity;\n    storyMap.forEach((entry) => { const element = document.querySelector(entry.cinematic); if (!element) return; const rect = element.getBoundingClientRect(); const distance = rect.bottom < headerOffset ? Math.abs(rect.bottom - headerOffset) + 180 : Math.abs(rect.top - headerOffset); if (distance < bestDistance) { bestDistance = distance; best = entry; } });\n    return best;\n  }\n  function nearestReadingEntry() {\n    const chapters = [...readingView.querySelectorAll('[data-seika-reading-chapter]')];\n    if (!chapters.length) return storyMap[0];\n    const headerOffset = 110; let active = chapters[0], bestDistance = Infinity;\n    chapters.forEach((chapter) => { const rect = chapter.getBoundingClientRect(); const distance = rect.bottom < headerOffset ? Math.abs(rect.bottom - headerOffset) + 180 : Math.abs(rect.top - headerOffset); if (distance < bestDistance) { bestDistance = distance; active = chapter; } });\n    const activeIndex = chapters.indexOf(active); let best = null;\n    storyMap.forEach((item) => { const target = document.getElementById(item.reading); const targetIndex = chapters.indexOf(target); if (targetIndex < 0) return; const distance = Math.abs(targetIndex - activeIndex); if (!best || distance < best.distance) best = { item, distance }; });\n    return best ? best.item : storyMap[0];\n  }\n  function setToggleState(mode) {\n    const reading = mode === 'reading'; body.dataset.loreMode = mode; toggle.setAttribute('aria-checked', String(reading)); toggle.setAttribute('aria-label', reading ? 'Ativar modo Cinemático' : 'Ativar modo Leitura'); toggle.title = reading ? 'Modo Leitura — trocar para Cinemático' : 'Modo Cinemático — trocar para Leitura'; if (status) status.textContent = reading ? 'Modo Leitura ativo.' : 'Modo Cinemático ativo.';\n  }\n  const jumpTo = (element) => element?.scrollIntoView({block:'start',behavior:'auto'});\n  function swapView(nextMode, entry) {\n    if (nextMode === 'reading') { cinematicView.hidden = true; readingView.hidden = false; setToggleState('reading'); jumpTo(document.getElementById(entry.reading) || readingView.querySelector('#seika-reading-top')); updateReadingProgress(); }\n    else { readingView.hidden = true; cinematicView.hidden = false; setToggleState('cinematic'); jumpTo(document.querySelector(entry.cinematic) || cinematicView); }\n    currentMode = nextMode;\n  }\n  async function changeMode(nextMode) {\n    if (nextMode === currentMode || body.classList.contains('is-switching')) return;\n    hideHint(); body.classList.add('is-switching'); toggle.setAttribute('aria-busy','true');\n    try {\n      if (nextMode === 'reading') await ensureReadingView();\n      const entry = currentMode === 'cinematic' ? nearestCinematicEntry() : nearestReadingEntry();\n      if (!reduceMotion.matches && document.startViewTransition) { const transition = document.startViewTransition(() => swapView(nextMode, entry)); await transition.finished; }\n      else swapView(nextMode, entry);\n    } catch (error) { console.error(error); if (status) status.textContent = 'Não foi possível abrir o modo Leitura.'; }\n    finally { body.classList.remove('is-switching'); toggle.removeAttribute('aria-busy'); toggle.focus({preventScroll:true}); }\n  }\n\n  toggle.addEventListener('click', () => changeMode(currentMode === 'cinematic' ? 'reading' : 'cinematic'));\n  setToggleState('cinematic');\n  document.querySelector('.brand')?.addEventListener('click', (event) => { if (currentMode !== 'reading') return; event.preventDefault(); window.scrollTo({top:0,behavior:reduceMotion.matches?'auto':'smooth'}); });\n\n  function setupReadingNavigation(view) {\n    const chapters = [...view.querySelectorAll('[data-seika-reading-chapter]')];\n    const links = [...view.querySelectorAll('[data-seika-reading-link]')];\n    chapterObserver?.disconnect();\n    chapterObserver = new IntersectionObserver((entries) => { const visible = entries.filter((entry) => entry.isIntersecting).sort((a,b) => Math.abs(a.boundingClientRect.top - 130) - Math.abs(b.boundingClientRect.top - 130)); if (!visible.length) return; const id = visible[0].target.id; links.forEach((link) => link.classList.toggle('active', link.dataset.seikaReadingLink === id)); }, {rootMargin:'-18% 0px -68% 0px',threshold:0});\n    chapters.forEach((chapter) => chapterObserver.observe(chapter));\n    view.addEventListener('click', (event) => { const link = event.target.closest('[data-seika-reading-link]'); if (!link) return; const mobile = link.closest('details'); if (mobile) mobile.open = false; });\n  }\n  function updateReadingProgress() {\n    if (currentMode !== 'reading' || !readingView) return;\n    const bar = readingView.querySelector('#seikaReadingProgressBar'); if (!bar) return;\n    const start = readingView.offsetTop, end = start + readingView.scrollHeight - window.innerHeight;\n    const progress = end <= start ? 1 : Math.min(1, Math.max(0, (window.scrollY - start) / (end - start)));\n    bar.style.width = `${progress * 100}%`;\n  }\n  window.addEventListener('scroll', updateReadingProgress, {passive:true});\n  window.addEventListener('resize', updateReadingProgress, {passive:true});\n\n  function hideHint() { if (!hint || hint.hidden) return; hint.hidden = true; if (hintTimer) clearTimeout(hintTimer); try { sessionStorage.setItem('lore-seika-mode-hint-seen','1'); } catch (_) {} }\n  try { if (hint && !sessionStorage.getItem('lore-seika-mode-hint-seen')) { window.setTimeout(() => { if (currentMode !== 'cinematic') return; hint.hidden = false; hintTimer = window.setTimeout(hideHint, 6500); }, 2600); } } catch (_) {}\n  hint?.addEventListener('click', hideHint);\n  window.requestIdleCallback?.(() => loadMarkdown().catch(() => {}), {timeout:4000});\n})();\n";

export function GET() {
  return new Response(BODY, {
    headers: {
      "Content-Type": "application/javascript; charset=utf-8",
      "Cache-Control": "public, max-age=300, s-maxage=3600",
      "Access-Control-Allow-Origin": "*",
    },
  });
}
